import React from 'react';

/**
 * Monitoring provides a dashboard for observing key workflow metrics such as
 * success rates, latency, and resource utilization. Future iterations can
 * render charts and allow custom SLO definitions.
 */
const Monitoring: React.FC = () => {
  return (
    <div>
      <h1>Monitoring</h1>
      <p>Metrics and monitoring dashboard will appear here.</p>
    </div>
  );
};

export default Monitoring;
